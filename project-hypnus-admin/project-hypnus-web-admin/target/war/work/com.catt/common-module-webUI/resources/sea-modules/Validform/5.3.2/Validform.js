/**
 * 引用Validform验证框架，使用此文件用于扩展验证框架中没有的时间处理
 */
define('Validform/5.3.2/Validform',
    [ '$','Validform/5.3.2/lib/Validform'],
    function(require, exports, module) {
        var demo = {
            version : '1.0'
        };
        console.log(module);
        console.log(demo);
    }
);
